import 'package:flutter/material.dart';

class MonthViewScreen extends StatelessWidget {
  const MonthViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Month View', style: TextStyle(fontSize: 24)),
    );
  }
}