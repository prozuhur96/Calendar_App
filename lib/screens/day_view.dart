import 'package:flutter/material.dart';

class DayViewScreen extends StatelessWidget {
  const DayViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Day View', style: TextStyle(fontSize: 24)),
    );
  }
}