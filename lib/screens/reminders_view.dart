import 'package:flutter/material.dart';

class RemindersViewScreen extends StatelessWidget {
  const RemindersViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Reminders View', style: TextStyle(fontSize: 24)),
    );
  }
}